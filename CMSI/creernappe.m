function res=creernappe()
    y=zeros(1000,1000);
    
    for x=1:1000
      %if any(x~=1:11) && any(x~=21:20:1000) && any(x~=20:20:1000) && any(x~=990:1000) && any(x~=847:849) && any(x~=827:829) && any(x~=807:809) && any(x~=165:173) && any(x~=185:193)
           y(x,x)=-4;
           if (x-1)>=1 %&& mod(x-1,20)>=1
               y(x,x-1)=1;
           end
           if x+1<=1000 %&& mod(x+1,21)>=1
               y(x,x+1)=1;
           end
           if x-20>=1 
               y(x,x-20)=1;
           end
           if x+20<=1000
               y(x,x+20)=1;
           end
           
       %end
       
    end
    for x=1:20
        y(x,1:1000)=0;
        y(x,x)=1;
        y(979+x,1:1000)=0;
        y(x+979,x+979)=1;
    end
    
    for x=1:23
        y(x*20,1:1000)=0;
        y(x*20,x*20)=1;
        
    end
    
    for x=0:10
        y(388+x*20,1:1000)=0;
        y(388+x*20,388+x*20)=1;
        y(389+x*20,1:1000)=0;
        y(389+x*20,389+x*20)=1;
        y(390+x*20,1:1000)=0;
        y(390+x*20,390+x*20)=1;
    end
    
    for x=1:24
        y(x*20+1,x*20+1)=-3;
        y(1000-(x*20+1),1000-(x*20+1))=-3;
    end
    for x=25:49
        y(x*20+1,1:1000)=0;
        y(x*20+1,x*20+1)=1;
    end
    
    y(1000,1:1000)=0;
    y(1000,1000)=1;
    
    res=y;

end
